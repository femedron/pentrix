package com.pentrix.game;

public class BestResult {
    public final int mode, bestScore, bestLevel;
    public BestResult(int m, int s, int l){
        mode = m; bestScore = s; bestLevel = l;
    }
}
